export const ROUTES = {
  HOME: "/",
  WORK: "/work",
  ABOUT: "/about",
  PLAYGROUND: "/playground",
  STACK: "/stack",
  CONTACT: "/contact",
} as const;

export const ROUTE_PATHS = Object.values(ROUTES);
